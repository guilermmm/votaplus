<%- model_class=Eleitor -%>

<% remote ||= remote %>
<% show_collapse ||= show_collapse %>


<div class="accordion" id="accordionEleitor">
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button <%= show_collapse.blank? ? 'collapsed' : '' %>" type="button" data-bs-toggle="collapse" data-bs-target="#eleitor" aria-expanded="<%= show_collapse.present? %>" aria-controls="Eleitor">
        <%= model_class.model_name.human(count: 2).titleize %>
      </button>
    </h2>
    <div id="eleitor" class="accordion-collapse collapse <%= show_collapse %>" data-bs-parent="#accordionEleitor">
      <div class="accordion-body">
        <%= render partial: "admin/eleitores/datatable" , locals: {remote: remote} %>
      </div>
    </div>
  </div>
</div>