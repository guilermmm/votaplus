<%- model_class=Bairro -%>

<% remote ||= remote %>
<% show_collapse ||= show_collapse %>


<div class="accordion" id="accordionBairro">
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button <%= show_collapse.blank? ? 'collapsed' : '' %>" type="button" data-bs-toggle="collapse" data-bs-target="#bairro" aria-expanded="<%= show_collapse.present? %>" aria-controls="Bairro">
        <%= model_class.model_name.human(count: 2).titleize %>
      </button>
    </h2>
    <div id="bairro" class="accordion-collapse collapse <%= show_collapse %>" data-bs-parent="#accordionBairro">
      <div class="accordion-body">
        <%= render partial: "admin/bairros/datatable" , locals: {remote: remote} %>
      </div>
    </div>
  </div>
</div>